apiVersion: apps/v1
kind: Deployment
metadata:
  name: apt-service
  namespace: default
  labels:
    app: apt-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apt-service
  template:
    metadata:
      labels:
        app: apt-service
    spec:
      containers:
        - name: apt-service
          image: whereis/apt-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - name: SERVER_PORT
              value: "8082"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mariadb://mariadb:3306/homedb?serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mariadb-secret
                  key: SPRING_DATASOURCE_PASSWORD
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: apt-service
  namespace: default
  labels:
    app: apt-service
spec:
  selector:
    app: apt-service
  type: NodePort
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
      protocol: TCP
